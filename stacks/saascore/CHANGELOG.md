# Changelog

## v0.1.0

- add first standard SaaS composition stack
- package reusable auth + email + billing + referral + shared user wiring
- expose typed config and route mounting helpers for host apps
